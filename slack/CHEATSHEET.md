# Slack cheatsheet — keyboard-only flow (HANDOFF §6)

Nothing to install or configure; this is pure adoption. `⌘/` inside Slack
lists everything — the table below is the subset worth making muscle memory.

## The core loop

| Keys | Does |
|---|---|
| `⌘K` | **Jump to anything** — channel, DM, person. The workhorse. |
| `⌘⇧A` | All unreads — triage inbox top to bottom |
| `⌘⇧K` | DMs list |
| `⌘[` / `⌘]` | History back / forward (like a browser) |
| `⌘⇧T` | Threads view |

Triage pattern: `⌘⇧A` → arrows between channels → read → `Esc` marks channel
read → next. Leave with `⌘K`.

## In a channel

| Keys | Does |
|---|---|
| `↑` / `↓` | Move between messages (when input is empty) |
| `↑` (in empty input) | Edit your last message |
| `T` | Open thread on selected message |
| `R` | Add reaction to selected message |
| `E` | Edit selected message (yours) |
| `Esc` | Mark channel read; `⇧Esc` marks **all** read |
| `⌘⇧\` | React to most recent message |

## Composing

| Keys | Does |
|---|---|
| `⌘⇧⏎` | New line vs send (or set Enter = newline in prefs) |
| `⌘B` / `⌘I` / `⌘⇧C` | Bold / italic / inline code |
| `⌘⇧8` | Bulleted list |

## With AeroSpace

Slack lives on workspace 3 (comms, Retina). Flow: `Hyper-3` → `⌘K` → type →
Enter. Two keystrokes from anywhere in the OS to any conversation.
